# Excel 模板放置规范

请将预算生成模板统一放在本目录：

- `templates/excel/unit_budget_template.xlsx`（单位口径）
- `templates/excel/department_budget_template.xlsx`（部门口径）

注意事项：

- 文件名必须与上述完全一致（区分大小写）。
- 仅支持 `.xlsx`。
- 系统会按草稿口径自动选择对应模板，不再扫描其他目录。
